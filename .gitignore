target/
nvim.log

# ASR model weights, when kept in the checkout; scripts/fetch-models.sh
# downloads them to $XDG_DATA_HOME/spokenpad/models.
models/

# Voice recordings: biometric data, kept local only.
eval-samples/*.wav
