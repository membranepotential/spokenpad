# Python / uv
.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ASR model weights (~630 MB) - fetched by scripts/fetch_model.py
models/

# Voice recordings: biometric data, kept local only.
# transcripts.json IS committed so the eval harness stays reproducible in shape.
eval-samples/*.wav
